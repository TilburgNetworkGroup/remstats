% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/remstats_durem.R
\name{.remstats_durem}
\alias{.remstats_durem}
\title{Internal remstats dispatch for \code{remify_durem} objects}
\usage{
.remstats_durem(
  reh,
  start_effects = NULL,
  end_effects = NULL,
  psi_start = 1,
  psi_end = 1,
  attr_actors = NULL,
  attr_dyads = NULL,
  memory = c("full", "window", "decay", "interval"),
  memory_value = NA,
  start = 2,
  stop = Inf,
  display_progress = FALSE
)
}
\arguments{
\item{reh}{A \code{remify_durem} object.}

\item{start_effects}{Formula for start-model statistics (remstats syntax).}

\item{end_effects}{Formula for end-model statistics.}

\item{psi_start}{Numeric. Duration exponent for start-model history
weighting. Event weight is \code{w * (end - time + 1)^psi_start}.
Default \code{1}.}

\item{psi_end}{Numeric. Duration exponent for end-model history weighting.
Default \code{1}.}

\item{attr_actors}{optionally, an object of class
\code{"\link[base]{data.frame}"} that contains exogenous attributes for 
actors (see Details).}

\item{attr_dyads}{optionally, an object of class \code{data.frame} or 
\code{matrix} containing attribute information for dyads (see Details).}

\item{memory}{The memory to be used. See `Details'.}

\item{memory_value}{Numeric value indicating the memory parameter. Default 
is \code{NA}, which is only valid for \code{memory = "full"} (no memory
parameter required). See `Details'.}

\item{display_progress}{should a progress bar for the computation of the
endogenous statistics be shown (TRUE) or not (FALSE)?}
}
\value{
A \code{remstats_durem} object.
}
\description{
Called by \code{\link{remstats}} when \code{reh} inherits from
\code{"remify_durem"}.  Builds a dual-event edgelist (one \code{"start"} row
and one \code{"end"} row per event), applies psi weighting, and calls
\code{tomstats} twice — once for the start model, once for the end model.
}
\keyword{internal}
