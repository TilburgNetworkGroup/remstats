% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/duremstats.R
\name{duremstats}
\alias{duremstats}
\title{Compute active-state statistics for a \code{remify_durem} object}
\usage{
duremstats(
  reh,
  start_effects = NULL,
  end_effects = NULL,
  start = 2L,
  stop = Inf,
  display_progress = FALSE
)
}
\arguments{
\item{reh}{A \code{remify_durem} object.}

\item{start_effects}{Formula of active-state effects for the start model,
e.g. \code{~ activeTie() + activeOutdegreeSender()}.}

\item{end_effects}{Formula of active-state effects for the end model.}

\item{start}{Integer. Index of first time point to compute (default 2).}

\item{stop}{Integer. Index of last  time point to compute (default Inf).}

\item{display_progress}{Logical. Show progress messages.}
}
\value{
A list with \code{$start_stats} and \code{$end_stats}: 3-D arrays
  \[M x D x P\] with effect names suffixed \code{.start} / \code{.end},
  and \code{attr(., "reh")} set to \code{reh}.  The same shape as a
  \code{remstats_durem} object so the two can be combined at estimation time.
}
\description{
Computes statistics that capture the current state of the active event
network at each time point.  These complement the history-weighted statistics
returned by \code{\link{remstats}} and cannot be derived from weighted
event history alone.
}
\details{
See \code{\link{active_effects}} for the full list of available effects
and their descriptions.
}
\keyword{internal}
